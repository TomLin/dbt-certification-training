*** Python Models - Packages and Logs Exceptions ***


- On dbt Fusion path to GA documentation:

		https://docs.getdbt.com/blog/dbt-fusion-engine-path-to-ga?utm



- Modify the python_model.py file like so:


import holidays

def is_holiday(date_col):
    # Chez Jaffle
    french_holidays = holidays.France()
    is_holiday = (date_col in french_holidays)
    return is_holiday

def model(dbt, session):

    dbt.config(packages=['holidays', 'pandas', 'pyarrow'])
    my_sql_model_df = dbt.ref("stablecoin_activity_per_day")

    my_sql_model_df = my_sql_model_df.to_pandas()

    my_sql_model_df["is_holiday"] = my_sql_model_df["DATE"].apply(is_holiday)



    return my_sql_model_df



- Back in our terminal:

		dbt run -m python_model




⚠️!!! If you do insert the "raise Exception..." line please don't forget to comment it out at the end of the lecture !!!⚠️